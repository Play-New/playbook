{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f CLAUDE.md ] && grep -q '## Playbook' CLAUDE.md 2>/dev/null && [ ! -d .playbook ]; then echo 'Project has playbook mapping in CLAUDE.md but .playbook/ directory is missing. Run /playbook:strategy to complete setup.'; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
